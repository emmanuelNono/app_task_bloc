enum DialogActionEnum { yes, abort, ok }
enum UserStateEnum { offline, online, waiting }
enum SortedByEnum { string, int }
enum TypeMessageEnum { text, image }
//page list des resultat
enum RencontrePageEnum { lastMonth, thisMonth, nextMonth }
enum ButtonStateEnum { init, loading, done }
